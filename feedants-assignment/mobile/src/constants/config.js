// Point this at your backend. When testing on a physical device or an
// Android emulator, "localhost" won't resolve to your dev machine - use
// your machine's LAN IP (e.g. http://192.168.1.23:5000) instead. iOS
// simulator can use localhost directly.
export const API_BASE_URL = "http://localhost:5000/api";

// Seeded ids are printed by `npm run seed` in the backend - update these
// after seeding, or wire up a real competitions-list screen.
export const DEMO_COMPETITION_ID = "REPLACE_WITH_SEEDED_COMPETITION_ID";
